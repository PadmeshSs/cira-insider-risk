"""Evaluation-side code (Chapter 6 onward).

This package is the only place where ground-truth labels meet feature rows,
and it does so in memory. Nothing here writes labels into
``<processed>/features/`` (CARRY_FORWARD N5).
"""
